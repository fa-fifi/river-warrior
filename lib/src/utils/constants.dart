import 'package:flame/components.dart';
import 'package:flutter/material.dart';

const gravity = -9.81;
const objSize = 50.0;
const acceleration = -400.0;
const gold = Color(0xFFFFD700);

final shapeSize = Vector2.all(50);
